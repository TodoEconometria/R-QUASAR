% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{qsr_write}
\alias{qsr_write}
\title{Write a data.frame to any supported format}
\usage{
qsr_write(data = NULL, path, format = "auto", name = "data", ...)
}
\arguments{
\item{data}{A data.frame, or NULL to use the registered context data.}

\item{path}{Character. Destination file; the extension picks the format.}

\item{format}{Character. \code{"auto"} (default) detects from the extension, or
force one of \code{csv}/\code{parquet}/\code{spss}/\code{stata}/\code{sas}/\code{excel}/\code{json}/\code{arrow}/\code{rds}/\code{rdata}.}

\item{name}{Character. For \code{.rdata}/\code{.rda}, the object name to store the
data.frame under. Default \code{"data"}.}

\item{...}{Passed to the underlying writer.}
}
\value{
The \code{path} (invisibly).
}
\description{
The write half of \code{\link[=qsr_read]{qsr_read()}}: exports a data.frame to the format implied by
the file extension, so the same framework that reads SPSS/Stata/Parquet/…
also writes them. This closes the round trip — read a survey, clean it, write
it back out (or to Parquet for a lakehouse, to \code{.sav} for a collaborator, to
\code{.rds} to resume later) with one call.
}
\details{
Supported extensions (mirroring \code{\link[=qsr_read]{qsr_read()}}):
\itemize{
\item \strong{CSV}: \code{.csv}, \code{.tsv}, \code{.txt} (via data.table if available, else base)
\item \strong{Parquet}: \code{.parquet}, \code{.pq} (via arrow)
\item \strong{SPSS}: \code{.sav} · \strong{Stata}: \code{.dta} · \strong{SAS transport}: \code{.xpt} (via haven)
\item \strong{Excel}: \code{.xlsx} (via writexl)
\item \strong{JSON}: \code{.json} (array) · \strong{NDJSON}: \code{.ndjson}, \code{.jsonl} (via jsonlite)
\item \strong{Arrow/Feather}: \code{.arrow}, \code{.feather} (via arrow)
\item \strong{Native R}: \code{.rds}, and \code{.rdata}/\code{.rda} (saved under \code{name})
}

\code{.sas7bdat} cannot be written (no open writer exists); use \code{.xpt} for SAS.
}
\examples{
\dontrun{
qsr_read("EPA2016.sav")
qsr_write(path = "epa_clean.parquet")   # to a lakehouse
qsr_write(path = "epa_clean.dta")       # to Stata for a collaborator
}

}
