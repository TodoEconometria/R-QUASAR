% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context.R
\name{qsr_model}
\alias{qsr_model}
\title{Fit a model with minimal syntax}
\usage{
qsr_model(
  formula,
  type = c("lm", "glm", "logit", "probit"),
  data = NULL,
  name = "last",
  family = NULL,
  weights = NULL,
  design = NULL,
  ...
)
}
\arguments{
\item{formula}{A model formula (e.g., \code{mpg ~ wt + hp}).}

\item{type}{Character. Model type: \code{"lm"}, \code{"glm"}, \code{"logit"}, \code{"probit"}.
Default \code{"lm"}.}

\item{data}{Optional data frame. If \code{NULL}, uses the active dataset
from context.}

\item{name}{Character. Name to store the model under. Default \code{"last"}.}

\item{family}{For GLM models. Ignored for \code{"lm"}.}

\item{weights}{Optional survey/regression weights: either the name of a
column in \code{data} or a numeric vector of length \code{nrow(data)}. Essential for
survey microdata, where estimates must use the elevation factor.}

\item{design}{Optional complex-survey design for \strong{design-based} inference:
a \code{survey.design} from \code{\link[=qsr_svydesign]{qsr_svydesign()}} (or \code{\link[survey:svrepdesign]{survey::svrepdesign()}}), or
\code{TRUE} to reuse the design registered by \code{\link[=qsr_svydesign]{qsr_svydesign()}}. Fits via
\code{\link[survey:svyglm]{survey::svyglm()}}, so the standard errors account for strata/clusters, not
just the weights. Supersedes \code{weights} and \code{data} when supplied.}

\item{...}{Additional arguments passed to the fitting function.}
}
\value{
The fitted model object (also stored in context).
}
\description{
Fits a statistical model using the active dataset from context.
No need to pass data - QUASAR already knows it.
}
\examples{
\dontrun{
qsr_data(mtcars)
qsr_model(mpg ~ wt + hp)
qsr_model(mpg ~ wt + hp + disp, name = "full")
qsr_model(am ~ wt + hp, type = "logit")

# Weighted regression on survey microdata:
qsr_read("EPA2016.sav")
qsr_model(parado ~ sexo + edad + educacion, weights = "FACTOREL")
}
}
