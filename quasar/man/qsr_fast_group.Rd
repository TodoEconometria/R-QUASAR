% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{qsr_fast_group}
\alias{qsr_fast_group}
\title{Fast group-by aggregation using Polars (Rust backend)}
\usage{
qsr_fast_group(
  path,
  by,
  col,
  fn = "mean",
  name = "grouped_data",
  register = TRUE
)
}
\arguments{
\item{path}{Character. Path to a CSV file.}

\item{by}{Character. Column name to group by.}

\item{col}{Character. Column name to aggregate.}

\item{fn}{Character. Aggregation function: "mean", "sum", "count", "min",
"max", "std".}

\item{name}{Character. Name for context registration. Default "grouped_data".}

\item{register}{Logical. Auto-register result in context. Default TRUE.}
}
\value{
A data.frame with grouped results (invisibly).
}
\description{
Performs group-by operations directly on a file using Polars - no need
to load the full dataset into R first.
}
\examples{
\dontrun{
# Average salary by department
qsr_fast_group("employees.csv", by = "department", col = "salary", fn = "mean")

# Then immediately plot or table it
qsr_table()
qsr_plot(type = "bar", x = department, y = salary_mean)
}

}
