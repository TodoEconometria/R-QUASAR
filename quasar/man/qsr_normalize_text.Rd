% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey.R
\name{qsr_normalize_text}
\alias{qsr_normalize_text}
\title{Normalize text fields with fuzzy matching}
\usage{
qsr_normalize_text(
  data = NULL,
  column,
  dictionary = NULL,
  max_dist = 2L,
  min_freq = 3L,
  review = TRUE,
  new_column = NULL,
  register = TRUE
)
}
\arguments{
\item{data}{Data frame or NULL (reads from context).}

\item{column}{Character. Name of the text column to normalize.}

\item{dictionary}{Character vector. Canonical values to match against.
If NULL, auto-generates from most frequent values.}

\item{max_dist}{Integer. Maximum Levenshtein distance for a match.
Default 2. Values >2 should be reviewed manually.}

\item{min_freq}{Integer. Minimum frequency to consider a value as
canonical when auto-generating dictionary. Default 3.}

\item{review}{Logical. Print matches for manual review before applying.
Default TRUE.}

\item{new_column}{Character. Name for normalized column. Default
paste0(column, "_clean").}

\item{register}{Logical. Default TRUE.}
}
\value{
Data frame with normalized column (invisibly).
}
\description{
Handles the "patiTo" problem: text libre in surveys with typos,
inconsistent capitalization, abbreviations, and dialect variants.
Uses Levenshtein distance for fuzzy matching with human review.
}
\examples{
\dontrun{
qsr_data(survey)
qsr_normalize_text(
  column = "crop_name",
  dictionary = c("coca", "cafe", "cacao", "banana", "arroz"),
  max_dist = 2,
  review = TRUE
)
}

}
