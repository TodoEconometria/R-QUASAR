% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey.R
\name{qsr_currency}
\alias{qsr_currency}
\title{Convert monetary values to constant USD}
\usage{
qsr_currency(
  data = NULL,
  columns,
  from_currency,
  base_year = 2015L,
  year_column = "year",
  exchange_rates = NULL,
  deflator = NULL,
  suffix = "_usd",
  register = TRUE
)
}
\arguments{
\item{data}{Data frame or NULL (reads from context).}

\item{columns}{Character vector. Columns with monetary values.}

\item{from_currency}{Character. Source currency code: "BOB", "PEN",
"COP", "BRL", etc.}

\item{base_year}{Integer. Base year for constant prices. Default 2015.}

\item{year_column}{Character. Column with the year. Default "year".}

\item{exchange_rates}{Named numeric vector or data frame. Exchange
rates (local currency per USD) by year. If NULL, uses built-in
rates from World Bank.}

\item{deflator}{Named numeric vector or data frame. CPI deflator by
year. If NULL, uses built-in deflators.}

\item{suffix}{Character. Suffix for new columns. Default "_usd".}

\item{register}{Logical. Default TRUE.}
}
\value{
Data frame with converted columns (invisibly).
}
\description{
Converts local currency values to USD using exchange rates and
deflators. Handles BOB, PEN, COP, BRL, and any other currency
with available exchange rate data.
}
\examples{
qsr_data(qsr_survey)
usd <- qsr_currency(
  columns = c("ingreso_laboral", "ingreso_total"),
  from_currency = "PEN",
  base_year = 2015,
  year_column = "year"
)
head(usd[, c("ingreso_laboral", "ingreso_laboral_usd")])
}
