% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml.R
\name{qsr_ml}
\alias{qsr_ml}
\title{Fit machine learning models with one line}
\usage{
qsr_ml(
  formula = NULL,
  data = NULL,
  method = c("rf", "xgboost", "svm", "nn", "knn", "lasso", "ridge", "elastic", "tree",
    "nb", "kmeans", "pca"),
  ...,
  name = "last",
  seed = NULL
)
}
\arguments{
\item{formula}{Formula (e.g., \code{mpg ~ .}). NULL reads from context.}

\item{data}{Data frame or NULL (reads from context).}

\item{method}{Character. ML method to use:
\describe{
\item{"rf"}{Random Forest (ranger)}
\item{"xgboost"}{Gradient Boosting (xgboost)}
\item{"svm"}{Support Vector Machine (e1071)}
\item{"nn"}{Neural Network (nnet - single hidden layer)}
\item{"knn"}{K-Nearest Neighbors (class)}
\item{"lasso"}{Lasso Regression (glmnet, alpha=1)}
\item{"ridge"}{Ridge Regression (glmnet, alpha=0)}
\item{"elastic"}{Elastic Net (glmnet, alpha=0.5)}
\item{"tree"}{Decision Tree (rpart)}
\item{"nb"}{Naive Bayes (e1071)}
\item{"kmeans"}{K-Means Clustering (stats)}
\item{"pca"}{Principal Component Analysis (stats)}
}}

\item{...}{Additional arguments passed to the underlying method.}

\item{name}{Character. Name for the model in context. Default "last".}

\item{seed}{Integer or NULL. Random seed. Default reads from context.}
}
\value{
The fitted model object (invisibly). Stored in context.
}
\description{
Unified interface for 12+ ML methods. Reads data from context,
trains the model, and stores it for downstream use with \code{qsr_table()},
\code{qsr_plot()}, and \code{qsr_compare()}.
}
\examples{
\dontrun{
qsr_data(mtcars)

# Random Forest
qsr_ml(mpg ~ ., method = "rf")

# XGBoost
qsr_ml(mpg ~ ., method = "xgboost")

# Neural Network
qsr_ml(mpg ~ wt + hp, method = "nn", size = 10)

# SVM
qsr_ml(am ~ wt + hp, method = "svm")

# Clustering (no formula needed)
qsr_ml(method = "kmeans", centers = 3)

# After fitting, everything works:
qsr_table()
qsr_plot(type = "importance")
}

}
