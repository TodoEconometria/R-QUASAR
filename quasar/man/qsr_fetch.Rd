% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connectors.R
\name{qsr_fetch}
\alias{qsr_fetch}
\title{Fetch data from academic APIs and URLs}
\usage{
qsr_fetch(source, ..., name = NULL, cache = TRUE)
}
\arguments{
\item{source}{Character. Data source: \code{"worldbank"}, \code{"fred"},
\code{"census"}, \code{"url"}.}

\item{...}{Arguments passed to the source-specific fetcher (e.g.,
\code{indicator}, \code{series_id}, \code{url}, \code{format}).}

\item{name}{Character. Name for context registration. If \code{NULL},
uses the source name.}

\item{cache}{Logical. Cache results for 24h to avoid repeat API calls.
Default \code{TRUE}.}
}
\value{
A data frame (invisibly), also registered in context.
}
\description{
One-line access to World Bank, FRED, Census, or any URL.
Data is auto-registered in context and optionally cached.
}
\examples{
\dontrun{
# World Bank GDP per capita
qsr_fetch("worldbank", indicator = "NY.GDP.PCAP.CD",
          country = "all", start = 2000, end = 2023)

# FRED unemployment rate
qsr_fetch("fred", series_id = "UNRATE")

# Any CSV from URL
qsr_fetch("url", url = "https://example.com/data.csv", format = "csv")
}
}
