% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/research.R
\name{qsr_download}
\alias{qsr_download}
\title{Download any research data file with one line}
\usage{
qsr_download(
  url,
  name = NULL,
  format = "auto",
  sheet = 1,
  cache = TRUE,
  cache_dir = tools::R_user_dir("rquasar", "cache"),
  auth = NULL,
  headers = NULL,
  retry = 3L,
  register = TRUE,
  ...
)
}
\arguments{
\item{url}{Character. URL to download from.}

\item{name}{Character. Name for the dataset in context. Default
auto-generated from URL.}

\item{format}{Character. Force format: "auto", "csv", "excel", "dta",
"sav", "parquet", "json", "zip". Default "auto" (detects from URL/content).}

\item{sheet}{Character or integer. Sheet name/number for Excel files.
Default 1.}

\item{cache}{Logical. Cache downloads to avoid re-downloading. Default TRUE.}

\item{cache_dir}{Character. Cache directory. Default a per-user cache dir.}

\item{auth}{Named list. Authentication: \code{list(type = "bearer", token = "xxx")} or \code{list(type = "basic", user = "x", pass = "y")}.
Default NULL (no auth).}

\item{headers}{Named list. Additional HTTP headers. Default NULL.}

\item{retry}{Integer. Number of retry attempts. Default 3.}

\item{register}{Logical. Auto-register in context. Default TRUE.}

\item{...}{Additional arguments passed to the reader function.}
}
\value{
A data.frame (invisibly). Registered in context.
}
\description{
Downloads files from any URL with automatic format detection, ZIP
extraction, retry logic, caching, and authentication. Handles Excel,
CSV, TSV, Stata (.dta), SPSS (.sav), Parquet, and ZIP archives
containing any of these formats.
}
\details{
Smart behaviors:
\itemize{
\item \strong{ZIP files}: Automatically extracts and finds data files inside
\item \strong{Stata/SPSS}: Reads .dta and .sav with labels preserved
\item \strong{Caching}: Won't re-download if file exists and is < 7 days old
\item \strong{Retry}: Exponential backoff on network failures
\item \strong{Large files}: Progress bar for downloads > 1MB
}
}
\examples{
\dontrun{
# Excel from UNODC
qsr_download("https://dataunodc.un.org/.../coca_data.xlsx",
             name = "unodc_coca")

# ZIP with Stata files inside (auto-extracts)
qsr_download("https://inei.gob.pe/.../enaho2023-601.zip",
             name = "enaho_2023")

# API with authentication
qsr_download("https://api.acleddata.com/acled/read.csv",
             auth = list(type = "params",
                         key = Sys.getenv("ACLED_API_KEY"),
                         email = Sys.getenv("ACLED_EMAIL")),
             name = "acled_events")

# After download, everything works:
qsr_clean(impute = "median")
qsr_model(y ~ x1 + x2)
qsr_table()
}

}
