% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deploy.R
\name{qsr_deploy}
\alias{qsr_deploy}
\title{Generate a Shiny app or Plumber API from your analysis}
\usage{
qsr_deploy(
  type = c("shiny", "api"),
  title = "QUASAR Dashboard",
  path = file.path(tempdir(), "rquasar_deploy"),
  models = NULL,
  include = c("data", "models", "plots", "tables", "predictions"),
  theme = c("flatly", "darkly", "cosmo", "default"),
  launch = FALSE
)
}
\arguments{
\item{type}{Character. Deploy type: "shiny", "api". Default "shiny".}

\item{title}{Character. App/API title. Default "QUASAR Dashboard".}

\item{path}{Character. Directory to write the app/API files. Default
"deploy/".}

\item{models}{Character vector. Model names from context to include.
Default NULL (all models).}

\item{include}{Character vector. What to include: "data", "models",
"plots", "tables", "predictions". Default all.}

\item{theme}{Character. Shiny theme: "default", "darkly", "flatly",
"cosmo". Default "flatly".}

\item{launch}{Logical. Launch the app after generating? Default FALSE.}
}
\value{
The path to the generated app (invisibly).
}
\description{
Creates a ready-to-run Shiny dashboard or REST API from the current
QUASAR context (data, models, plots, tables). No Shiny/Plumber
knowledge required.
}
\examples{
\dontrun{
qsr_data(mtcars)
qsr_model(mpg ~ wt + hp)
qsr_table()
qsr_plot(type = "coefficient")

# Generate Shiny dashboard
qsr_deploy()

# Generate REST API
qsr_deploy(type = "api", path = "my_api/")

# Generate and launch
qsr_deploy(launch = TRUE)
}

}
