% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connectors.R
\name{qsr_python}
\alias{qsr_python}
\title{Run Python code from R}
\usage{
qsr_python(
  package,
  fn = NULL,
  args = list(),
  fit = NULL,
  predict = NULL,
  envname = "quasar-py",
  install = FALSE,
  name = "python_result",
  ...
)
}
\arguments{
\item{package}{Character. Python module to import (e.g., \code{"prophet"},
\code{"sklearn.linear_model"}).}

\item{fn}{Character. Function or class name to call. If \code{NULL},
returns the imported module.}

\item{args}{Named list. Arguments to pass to \code{fn()}.}

\item{fit}{List. If provided, calls \code{.fit()} on the result.
For sklearn: \code{list(X, y)}. For single-arg: \code{list(df)}.}

\item{predict}{Data to predict on. If provided, calls \code{.predict()}
and registers the result in context.}

\item{envname}{Character. Python virtualenv name. Default \code{"quasar-py"}.}

\item{install}{Logical. If \code{TRUE}, pip-installs the package first.}

\item{name}{Character. Name for context registration of predictions.}

\item{...}{Additional arguments.}
}
\value{
The Python object or prediction results (invisibly).
}
\description{
Zero-friction Python interop. Import modules, call functions,
fit sklearn/prophet models — all without touching reticulate directly.
}
\examples{
\dontrun{
# Simple function call
qsr_python("math", fn = "sqrt", args = list(16))

# Import a module
np <- qsr_python("numpy")

# Fit a sklearn model
qsr_python("sklearn.linear_model", fn = "LinearRegression",
           fit = list(X_train, y_train), predict = X_test)
}
}
