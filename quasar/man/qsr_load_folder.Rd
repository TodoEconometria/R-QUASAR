% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/research.R
\name{qsr_load_folder}
\alias{qsr_load_folder}
\title{Load all data files from a folder}
\usage{
qsr_load_folder(
  path,
  pattern = NULL,
  recursive = FALSE,
  stack = FALSE,
  extract_zips = TRUE,
  name = "folder_data",
  register = TRUE
)
}
\arguments{
\item{path}{Character. Path to the folder.}

\item{pattern}{Character. Regex to filter filenames. Default NULL
reads all supported formats.}

\item{recursive}{Logical. Search subfolders. Default FALSE.}

\item{stack}{Logical. If TRUE, stack all data frames with
\code{rbind(fill = TRUE)} and add a \verb{_source} column. Default FALSE.}

\item{extract_zips}{Logical. Auto-extract ZIP files. Default TRUE.}

\item{name}{Character. Context name (used when stack = TRUE).
Default "folder_data".}

\item{register}{Logical. Default TRUE.}
}
\value{
A named list of data frames, or a single stacked data frame
if \code{stack = TRUE} (invisibly).
}
\description{
Reads every data file in a directory: ZIP (auto-extracts), CSV,
Excel, Stata (.dta), SPSS (.sav), Parquet. Returns a named list
of data frames indexed by filename, and optionally stacks them.
}
\details{
Designed for the workflow: "download manually, point QUASAR at
the folder, let it handle everything."
}
\examples{
\dontrun{
# Point QUASAR at your manually downloaded Bolivia surveys
surveys <- qsr_load_folder(
  "E:/quasar_data/coca_research/raw/bolivia/",
  extract_zips = TRUE
)

# Stack all years into one panel
panel <- qsr_load_folder(
  "E:/quasar_data/coca_research/raw/bolivia/",
  pattern = "Persona",
  stack = TRUE,
  name = "eh_personas"
)
}

}
