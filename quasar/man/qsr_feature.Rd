% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{qsr_feature}
\alias{qsr_feature}
\title{Automatic feature engineering}
\usage{
qsr_feature(
  data = NULL,
  interactions = NULL,
  polynomial = NULL,
  log = NULL,
  bins = NULL,
  lag = NULL,
  name = "data",
  register = TRUE
)
}
\arguments{
\item{data}{Data frame or NULL (reads from context).}

\item{interactions}{Character vector of column pairs (e.g., c("x1:x2")).}

\item{polynomial}{Character vector of columns + degree (e.g., c("x1" = 2)).}

\item{log}{Character vector of columns to log-transform.}

\item{bins}{List of columns to bin (e.g., list(age = 4)).}

\item{lag}{List of columns to lag (e.g., list(value = 1)).}

\item{name}{Character. Context name. Default "data".}

\item{register}{Logical. Default TRUE.}
}
\value{
Data frame with new features (invisibly).
}
\description{
Create new features from existing columns: interactions, polynomials,
lag/lead, binning, log transforms.
}
\examples{
\dontrun{
qsr_data(mtcars)
qsr_feature(log = c("hp", "wt"), interactions = c("wt:hp"),
            polynomial = c(wt = 2))
}

}
