% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output.R
\name{qsr_table}
\alias{qsr_table}
\title{Create publication-ready tables}
\usage{
qsr_table(
  x = NULL,
  type = NULL,
  format = NULL,
  caption = NULL,
  export = NULL,
  output_dir = "outputs/tables",
  stars = TRUE,
  notes = NULL,
  model = "last",
  ...
)
}
\arguments{
\item{x}{A model object, data frame, or \code{NULL} to use context.
If omitted and a model exists in context, uses that model.}

\item{type}{Character. Table type: \code{"model"} for regression tables,
\code{"summary"} for descriptive statistics, \code{"custom"} for raw data frames.
Defaults to auto-detection.}

\item{format}{Character. Output style. If \code{NULL}, reads from context
(\code{output_format}). One of \code{"apa7"}, \code{"chicago"}, \code{"custom"}.}

\item{caption}{Character. Table caption/title.}

\item{export}{Character vector. Export formats: any combination of
\code{"html"}, \code{"latex"}, \code{"word"}, \code{"rtf"}. If \code{NULL}, only returns the
gt object for interactive use.}

\item{output_dir}{Character. Directory for exported files. Defaults to
\code{"outputs/tables"}.}

\item{stars}{Logical. Show significance stars. Default \code{TRUE}.}

\item{notes}{Character vector. Footnotes to append below the table.}

\item{model}{Character. Name of stored model to use. Default \code{"last"}.}

\item{...}{Additional arguments passed to \code{\link[gtsummary:tbl_regression]{gtsummary::tbl_regression()}}
or \code{\link[gt:gt]{gt::gt()}}.}
}
\value{
A \code{gt} table object (invisibly if exported).
}
\description{
Generates formatted tables from statistical models or data frames,
ready for academic publication. If called with no arguments, uses the
last model from context. If only \code{type = "summary"} is given, uses
the active dataset from context.
}
\examples{
\dontrun{
# Classic usage
model <- lm(mpg ~ wt + hp, data = mtcars)
qsr_table(model, caption = "Table 1. OLS estimates")

# Zero-argument usage (after qsr_data + qsr_model)
qsr_data(mtcars)
qsr_model(mpg ~ wt + hp)
qsr_table()                    # uses last model
qsr_table(type = "summary")   # uses active dataset
}
}
