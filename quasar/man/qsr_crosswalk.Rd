% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey.R
\name{qsr_crosswalk}
\alias{qsr_crosswalk}
\title{Apply a geographic crosswalk}
\usage{
qsr_crosswalk(
  data = NULL,
  crosswalk,
  column,
  year_column = NULL,
  apply_before = NULL,
  apply_after = NULL,
  new_column = NULL,
  register = TRUE
)
}
\arguments{
\item{data}{Data frame or NULL (reads from context).}

\item{crosswalk}{Data frame with columns \code{old} and \code{new}, or path
to a CSV file with the crosswalk table.}

\item{column}{Character. Column in data containing the geographic code.}

\item{year_column}{Character. Column with the year (to apply crosswalk
only to specific years). Default NULL (apply to all).}

\item{apply_before}{Integer. Apply crosswalk to years before this.
Default NULL.}

\item{apply_after}{Integer. Apply crosswalk to years after this.
Default NULL.}

\item{new_column}{Character. Name for harmonized column. Default
paste0(column, "_harmonized").}

\item{register}{Logical. Default TRUE.}
}
\value{
Data frame with harmonized geographic codes (invisibly).
}
\description{
Translates geographic codes between periods (e.g., ubigeo pre/post
2017 in Peru, DIVIPOLA changes in Colombia, municipio codes in Bolivia).
}
\examples{
\dontrun{
# Peru ubigeo crosswalk
qsr_crosswalk(
  column = "ubigeo",
  crosswalk = "data/raw/spatial/peru/ubigeo_crosswalk_2017.csv",
  year_column = "year",
  apply_before = 2017
)

# Colombia DIVIPOLA
qsr_crosswalk(
  column = "cod_municipio",
  crosswalk = data.frame(old = c("05001", "05002"), new = c("05001", "05899"))
)
}

}
