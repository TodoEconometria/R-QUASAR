% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output.R
\name{qsr_plot}
\alias{qsr_plot}
\title{Create publication-ready plots with QUASAR theme}
\usage{
qsr_plot(
  data = NULL,
  type = "scatter",
  x = NULL,
  y = NULL,
  z = NULL,
  color = NULL,
  size = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  interactive = FALSE,
  theme = "quasar_academic",
  export = NULL,
  output_dir = file.path(tempdir(), "rquasar_figures"),
  width = NULL,
  height = NULL,
  dpi = 300,
  ...
)
}
\arguments{
\item{data}{A data frame, model object, ggplot object, or \code{NULL} to use
context. For \code{"coefficient"} type, uses last model from context.
For other types, uses active dataset from context.}

\item{type}{Character. Plot type. \strong{Static types} (ggplot2): \code{"scatter"},
\code{"bar"}, \code{"histogram"}, \code{"density"}, \code{"boxplot"}, \code{"coefficient"},
\code{"correlation"}. \strong{Native interactive types} (plotly): \code{"scatter3d"},
\code{"surface3d"}, \code{"bubble"}, \code{"heatmap"}, \code{"violin"}, \code{"ridge"},
\code{"parallel"}, \code{"sankey"}, \code{"treemap"}, \code{"sunburst"}, \code{"funnel"},
\code{"waterfall"}, \code{"radar"}.}

\item{x, y, z}{Column names (unquoted or character) for axes.
\code{z} is used only by 3D plot types.}

\item{color}{Optional column name for color/group aesthetic.}

\item{size}{Optional column name for size aesthetic (bubble plots).}

\item{title}{Character. Plot title.}

\item{subtitle}{Character. Plot subtitle.}

\item{caption}{Character. Plot caption/source note.}

\item{interactive}{Logical. If \code{TRUE}, converts ggplot output to an
interactive plotly chart with hover, zoom, and pan. Default \code{FALSE}.}

\item{theme}{Character. Theme variant: \code{"quasar_academic"} (default),
\code{"quasar_minimal"}, \code{"quasar_dark"}.}

\item{export}{Character vector. Export formats: \code{"png"}, \code{"pdf"}, \code{"svg"}
for static; \code{"html"} for interactive. If \code{NULL}, only displays the plot.}

\item{output_dir}{Character. Directory for exported files.}

\item{width, height}{Numeric. Plot dimensions in inches (static) or
pixels (interactive). Defaults: 7x5 inches / 800x500 pixels.}

\item{dpi}{Numeric. Resolution for raster exports. Default 300.}

\item{...}{Additional arguments passed to the geom or plotly trace.}
}
\value{
A \code{ggplot} object or \code{plotly} htmlwidget.
}
\description{
Wraps ggplot2 and plotly with a consistent academic theme. If called with
no data argument, uses the active dataset or last model from context.
}
\examples{
\dontrun{
# Zero-argument (after qsr_data + qsr_model)
qsr_data(mtcars)
qsr_model(mpg ~ wt + hp)
qsr_plot()                        # scatter of first 2 numeric cols
qsr_plot(type = "coefficient")    # coefficient plot from last model
qsr_plot(type = "correlation")    # correlation of active dataset

# Explicit data
qsr_plot(mtcars, type = "scatter", x = wt, y = mpg, interactive = TRUE)

# Re-theme an existing ggplot
p <- ggplot2::ggplot(mtcars, ggplot2::aes(wt, mpg)) +
       ggplot2::geom_point()
qsr_plot(p, interactive = TRUE)
}
}
