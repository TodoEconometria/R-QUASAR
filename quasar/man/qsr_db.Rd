% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connectors.R
\name{qsr_db}
\alias{qsr_db}
\title{Connect to a database and load data}
\usage{
qsr_db(
  driver = c("sqlite", "postgres", "mysql", "odbc"),
  dbname = NULL,
  host = "localhost",
  port = NULL,
  user = NULL,
  password = NULL,
  query = NULL,
  table = NULL,
  name = "db_data",
  .conn = FALSE,
  ...
)
}
\arguments{
\item{driver}{Character. Database driver: \code{"sqlite"}, \code{"postgres"},
\code{"mysql"}, \code{"odbc"}. Default \code{"sqlite"}.}

\item{dbname}{Character. Database name or file path (SQLite).
Defaults to \code{":memory:"} for SQLite.}

\item{host}{Character. Server hostname. Default \code{"localhost"}.}

\item{port}{Integer. Port number. Auto-detected from driver if \code{NULL}.}

\item{user}{Character. Username. If \code{NULL}, reads from env var
\code{QUASAR_DB_USER}.}

\item{password}{Character. Password. If \code{NULL}, reads from env var
\code{QUASAR_DB_PASSWORD}.}

\item{query}{Character. SQL query to execute. Mutually exclusive with
\code{table}.}

\item{table}{Character. Table name to read entirely. Mutually exclusive
with \code{query}.}

\item{name}{Character. Name for context registration. Default \code{"db_data"}.}

\item{.conn}{Logical. If \code{TRUE}, returns the raw DBI connection instead
of auto-querying. Default \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[DBI:dbConnect]{DBI::dbConnect()}}.}
}
\value{
A data frame (invisibly) or a DBI connection if \code{.conn = TRUE}.
}
\description{
One-line database access. Connects, queries, and registers the result
in context automatically. Uses SQLite in-memory by default - zero setup.
}
\examples{
\dontrun{
# In-memory SQLite (zero setup)
qsr_db("sqlite")

# Read a table
qsr_db("sqlite", dbname = "research.sqlite", table = "surveys")

# Run a query
qsr_db("postgres", dbname = "research",
       query = "SELECT * FROM surveys WHERE year > 2020")
}
}
