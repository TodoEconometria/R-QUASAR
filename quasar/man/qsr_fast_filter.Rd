% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{qsr_fast_filter}
\alias{qsr_fast_filter}
\title{Fast filter using Polars (Rust backend)}
\usage{
qsr_fast_filter(path, col, op, value, name = "filtered_data", register = TRUE)
}
\arguments{
\item{path}{Character. Path to a CSV file.}

\item{col}{Character. Column name to filter on.}

\item{op}{Character. Operator: "gt" (>), "lt" (<), "gte" (>=), "lte" (<=),
"eq" (==), "neq" (!=).}

\item{value}{Numeric. Value to compare against.}

\item{name}{Character. Name for context registration. Default "filtered_data".}

\item{register}{Logical. Auto-register result in context. Default TRUE.}
}
\value{
A data.frame with filtered results (invisibly).
}
\description{
Filters rows from a file directly using Polars — processes the file
without loading it entirely into R memory.
}
\examples{
\dontrun{
# Filter large dataset without loading into memory
qsr_fast_filter("data/sales.csv", col = "amount", op = "gt", value = 1000)

# Chain: filter → table
qsr_fast_filter("data/survey.csv", col = "age", op = "gte", value = 18)
qsr_table(type = "summary")
}

}
