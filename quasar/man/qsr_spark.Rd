% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connectors.R
\name{qsr_spark}
\alias{qsr_spark}
\title{Connect to Apache Spark}
\usage{
qsr_spark(
  master = "local",
  memory = "4G",
  read = NULL,
  name = "spark_data",
  version = NULL,
  ...
)
}
\arguments{
\item{master}{Character. Spark master URL. Default \code{"local"}.}

\item{memory}{Character. Memory for driver and executor. Default \code{"4G"}.}

\item{read}{Character. Optional path to a file to load (CSV, Parquet, JSON).
Format auto-detected from extension.}

\item{name}{Character. Name for context registration. Default \code{"spark_data"}.}

\item{version}{Character. Spark version. If \code{NULL}, uses installed version.}

\item{...}{Additional arguments passed to \code{sparklyr::spark_config()}.}
}
\value{
Invisible Spark connection, or a Spark DataFrame if \code{read} is given.
}
\description{
One-line Spark setup. Connects with sensible defaults and optionally
loads data in a single call.
}
\examples{
\dontrun{
qsr_spark()                                # local, 4G
qsr_spark(master = "yarn", memory = "8G")  # cluster
qsr_spark(read = "data/big_file.csv")      # connect + load
}
}
