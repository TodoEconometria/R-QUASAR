% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{.qsr_transcode_utf8}
\alias{.qsr_transcode_utf8}
\title{Transcode a text file to a temporary UTF-8 copy (streaming, constant memory)}
\usage{
.qsr_transcode_utf8(path, from, n_lines = NULL, chunk = 32L * 1024L^2)
}
\arguments{
\item{path}{Character. Source file path.}

\item{from}{Character. Source encoding (e.g. "latin1").}

\item{n_lines}{Integer or NULL. If set, transcode only the first \code{n_lines} + 1
lines (header + rows) instead of the whole file -- keeps sampling cheap on huge files.}

\item{chunk}{Integer. Bytes per chunk for the full-file path. Default 32 MiB.}
}
\value{
Character. Path to the temporary UTF-8 file (caller unlinks it).
}
\description{
Polars assumes UTF-8. Source files in single-byte encodings (latin1/ISO-8859-1,
windows-1252) are converted here in binary chunks so accents and " n-tilde" survive
and memory stays flat regardless of file size. Returns the temp file path.
}
\keyword{internal}
