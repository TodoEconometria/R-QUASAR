% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{qsr_test}
\alias{qsr_test}
\title{Run statistical tests with one line}
\usage{
qsr_test(
  formula = NULL,
  data = NULL,
  test = c("auto", "t", "paired_t", "anova", "chi2", "wilcox", "kruskal", "shapiro",
    "correlation", "levene", "fisher"),
  ...
)
}
\arguments{
\item{formula}{Formula (e.g., \code{y ~ group}) or NULL.}

\item{data}{Data frame or NULL (reads from context).}

\item{test}{Character. Test type: "auto", "t", "paired_t", "anova",
"chi2", "wilcox", "kruskal", "shapiro", "correlation", "levene",
"fisher". Default "auto".}

\item{...}{Additional arguments passed to the underlying test function.}
}
\value{
A list with test results (invisibly). Also stored in context.
}
\description{
Auto-detects the appropriate test based on the data, or specify one
explicitly. Results are printed and stored in context.
}
\details{
When \code{test = "auto"}, QUASAR chooses the test based on the formula:
\itemize{
\item Numeric ~ Factor (2 levels): t-test
\item Numeric ~ Factor (3+ levels): ANOVA
\item Factor ~ Factor: chi-squared
\item Single numeric variable: Shapiro-Wilk normality test
\item Two numeric variables: Pearson correlation
}
}
\examples{
\dontrun{
qsr_data(mtcars)

# Auto-detect: t-test (mpg by transmission type)
qsr_test(mpg ~ am)

# Auto-detect: ANOVA (mpg by number of cylinders)
qsr_test(mpg ~ cyl)

# Explicit test
qsr_test(mpg ~ am, test = "wilcox")

# Normality test
qsr_test(~mpg, test = "shapiro")

# Correlation
qsr_test(mpg ~ wt, test = "correlation")
}

}
