% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml.R
\name{qsr_cv}
\alias{qsr_cv}
\title{Cross-validate a model with one line}
\usage{
qsr_cv(
  formula = NULL,
  data = NULL,
  method = c("rf", "xgboost", "svm", "nn", "lasso", "ridge", "elastic", "tree"),
  k = 10L,
  ...
)
}
\arguments{
\item{formula}{Formula or NULL (reads from context).}

\item{data}{Data frame or NULL (reads from context).}

\item{method}{Character. ML method (same as \code{qsr_ml()}).}

\item{k}{Integer. Number of folds. Default 10.}

\item{...}{Additional arguments passed to the underlying method.}
}
\value{
A list with CV results (invisibly).
}
\description{
Performs k-fold cross-validation on the last model or a specified method.
Reports accuracy (classification) or RMSE (regression).
}
\examples{
\dontrun{
qsr_data(mtcars)
qsr_cv(mpg ~ ., method = "rf", k = 5)
qsr_cv(am ~ wt + hp, method = "nn", size = 5, k = 10)
}

}
