% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/research.R
\name{qsr_panel}
\alias{qsr_panel}
\title{Build a research panel from multiple data sources}
\usage{
qsr_panel(
  ...,
  join_by,
  sources = NULL,
  balance = FALSE,
  rename = NULL,
  name = "panel",
  register = TRUE
)
}
\arguments{
\item{...}{Named data frames or names of datasets in context.}

\item{join_by}{Character vector. Variables to join by (e.g.,
\code{c("country", "year")}).}

\item{sources}{Character vector. Names of datasets in context to use.
Alternative to passing data frames in \code{...}.}

\item{balance}{Logical. Balance the panel (fill missing combinations
with NA). Default FALSE.}

\item{rename}{Named list. Rename columns across sources before joining
(e.g., \code{list(Country = "country", Year = "year")}).}

\item{name}{Character. Name for the panel in context. Default "panel".}

\item{register}{Logical. Default TRUE.}
}
\value{
A data.frame panel (invisibly).
}
\description{
Joins multiple datasets by key variables (country+year, id+time, etc.)
into a single analysis-ready panel. Handles different column names,
missing combinations, and reports coverage statistics.
}
\details{
The panel builder:
\enumerate{
\item Standardizes column names across sources (via \code{rename})
\item Joins all sources by \code{join_by} keys
\item Reports coverage: rows, sources merged, completeness per variable
\item Optionally balances the panel (all key combinations)
}
}
\examples{
\dontrun{
# Download from multiple sources
qsr_download("https://dataunodc.un.org/.../coca.xlsx", name = "unodc")
qsr_fetch("worldbank", indicator = "NY.GDP.PCAP.KD",
          country = c("BOL", "PER", "COL"), name = "wb_gdp")

# Build panel - joins by country + year
qsr_panel(
  sources = c("unodc", "wb_gdp"),
  join_by = c("country", "year"),
  rename  = list(Country = "country", Year = "year"),
  balance = TRUE
)

# Now analyze
qsr_model(coca_area ~ gdp_pc + poverty_gap)
qsr_table()
}

}
